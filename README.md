# C-playground 

A from-scratch implementation of the POSIX tools like `ls` command in C.

# Currently 
- Only `ls` for now but will continue to expand this
- This repo is just for learning purposes and act as a tangible remainder of my C journey
